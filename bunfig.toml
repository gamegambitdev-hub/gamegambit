# Bun configuration for Game Gambit Next.js project
# Configured to regenerate lock files for Next.js instead of using stale React Native lock

[install]
# Allow Bun to regenerate dependencies
save-lockfile = true
# Peer dependencies won't block installation
optional = true
